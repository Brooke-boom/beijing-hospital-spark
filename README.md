# 基于Spark的北京市医疗机构资源整合与多维筛选可视化系统

本科毕业设计作品。整合北京市 9,678 家医疗机构的多源公开数据，基于 Spark 构建离线数仓。

系统的主线是**「自然语言智能筛选」**，不是"推荐医院"：

1. **智能筛选（任务主线）** — 说一句话（"朝阳和海淀的三级医院"）或直接选条件 → 解析成结构化**条件卡** → 查询真实数据 → 得到命中清单、KPI 与地图分布；说"按行政区统计"则切换为**维度统计**（图表 + 分组 KPI）。所有数字都来自数据库查询，AI 只负责把中文翻译成条件。
2. **查阅支撑页（4 个）** — 找机构 / 资源画像 / 数据治理 / 系统说明，回答"有哪些机构、资源怎么分布、数据从哪来"。

系统**不做医疗建议**：病征类输入一律硬拒答——本项目的数据是机构资源数据，没有患者诊疗数据。

两条线都有**两种形态**：在线（Vue 3 + Flask，数据可实时更新）与**零依赖单文件**
（`web/dashboard_offline.html`，一个 HTML 双击即用）。单文件形态同样具备完整主线——
解析与查询由前端本地完成（词表从后端同一份真源导出后内联，`etl/verify_offline_nlq.py` 351 项断言把关），
因此**分享一个链接就能让别人的机器走完整个筛选流程**。

## 技术栈

| 层 | 技术 |
|---|---|
| 数据处理 | PySpark 3.5.3（默认 `local[*]` 单机模式，可用 `SPARK_MASTER` 切回 Standalone 集群；Docker 原生 arm64） |
| 存储 | **HDFS 3.3.6**（ODS/DWD/DWS 分层 Parquet）+ **MySQL 8.0**（ADS 服务层结果） |
| 依赖管理 | Maven 坐标（`spark-submit --packages`，见 `spark/pom.xml`） |
| 服务 | Flask RESTful API（25 个 API 接口 / 28 个路由） |
| 前端 | **Vue 3 + Vite**（前后端分离，`/spa/`）：1 条任务主线 + 4 个查阅支撑页；ECharts 5；另保留零依赖单文件离线大屏 |
| 部署 | Docker Compose |

## 目录结构

```
├── data/               # 数据目录（不入库，含原始数据与 processed 产物）
├── etl/                # 数据清洗/地理编码/科室字典脚本 + 入湖脚本
│   └── upload_to_hdfs.py   # 治理后 CSV / 行为日志 → HDFS ods/raw（数据入湖）
├── spark/              # PySpark 数仓作业
│   ├── jobs/           # 按「分层 + 分析维度」拆分（见下表）
│   ├── run_all.sh      # 全链路一键跑批（宿主机）
│   └── pom.xml         # 依赖清单（Maven 坐标）
├── docker/hadoop/      # 自建 arm64 HDFS 镜像（官方镜像仅 amd64，M1 上起不来）
├── web/                # Flask 应用（API）+ 两种前端形态
│   ├── app.py          # RESTful API（28 个路由：自然语言筛选/维度统计/筛选/排序/详情/概览/对比/地理编码/埋点）
│   ├── nlq.py          # 自然语言筛选规则引擎（词表 + 条件解析 + 参数化 SQL + 维度统计）——全系统唯一口径真源
│   ├── app.nlq.js      # 单文件形态的离线解析/筛选引擎（与 nlq.py 同源）
│   ├── app.nlq.ui.js   # 单文件形态的智能筛选界面层
│   ├── nlq_lexicon.json# 构建时从 nlq.py 导出的词表（内联进产物，禁止手抄）
│   ├── vue/            # Vue 3 前端工程（前后端分离形态，构建产物挂载在 /spa/）
│   │   ├── src/        # 源码：9 视图（SmartFilterView 主线 + HubView 支撑页 + 子视图）+ 6 组件
│   │   ├── build.sh    # 一键构建 / 启动 dev server
│   │   └── dist/       # 构建产物（已入库，Flask 挂载点）
│   ├── templates/      # 单文件形态的 Flask 渲染版（与离线版同源）
│   └── static/         # ECharts、北京 geoJSON、样式
├── docs/               # 开发日志、项目说明书、功能点代码地图等文档
└── docker-compose.yml  # hdfs(namenode+datanode) + spark(master+worker) + mysql
```

### Spark 作业按分层 + 分析维度拆分

| 文件 | 职责 | 输入 → 输出 |
|---|---|---|
| `jobs/common.py` | 公共模块：SparkSession、HDFS 路径、读写封装 | — |
| `jobs/layer_ods.py` | ODS 层：原始数据列式化 | HDFS CSV → HDFS Parquet |
| `jobs/layer_dwd.py` | DWD 层：清洗 + 坐标关联 | HDFS Parquet → HDFS Parquet |
| `jobs/dim_space.py` | **空间维度**：区县分布、坐标覆盖、区间 Haversine 距离矩阵 | DWD → `dws/space/*` |
| `jobs/dim_category.py` | **类型/等级维度**：等级、类型、主办方结构 | DWD → `dws/category/*` |
| `jobs/dim_dept.py` | **科室维度**：科室覆盖度、科室×区分布、科室密度 | DWD → `dws/dept/*` |
| `jobs/dept_dict.py` | 科室名归一字典：200+ 种写法 → 49 个标准科室 + 大类 | — |
| `jobs/dim_dept_strength.py` | **科室实力指数**：national/municipal/feature/登记四来源归一 → 五级证据分级（国家/市级/官网/登记/已开设）+ 专科集中度（**下线主线遗留：作业仍产出，前端已无引用**） | DWD → `dws/dept/dept_strength` + MySQL `ads_dept_strength` |
| `jobs/dim_network.py` | **协作网络维度**：儿科医联体/卒中/危重新生儿/危重孕产妇 | DWD → `dws/network/*` |
| `jobs/dim_time.py` | **时间维度**：用户行为日趋势/时段/功能结构 + ETL 批次时效性快照 | 行为日志 → `dws/time/*` |
| `jobs/layer_ads.py` | ADS 服务层：分析结果落业务库 | HDFS → MySQL |
| `jobs/run_all.py` | 编排入口（单 SparkSession 串起 10 个阶段，支持 `--only` / `--from`） | — |

## 快速开始

### 方式一：零依赖单文件形态（30 秒，推荐先看效果）

```bash
open web/dashboard_offline.html      # macOS；其他系统直接双击该文件
```

数据快照、词表与筛选引擎已内联进单个 HTML（约 10MB），**无需 Python、Docker、数据库、联网**，
即可使用自然语言筛选、七维条件筛选、五种排序、维度统计、图表联动与详情查看。

> 这是**单文件形态**，查阅与智能筛选两种能力都具备：侧栏「智能筛选」是完整主线
> （说一句话 → 条件卡 → 真实数据结果），解析与查询由 `web/app.nlq.js` 在本地完成，
> 结果与在线接口 `/api/nlq/*` 逐字段一致（由 `etl/verify_offline_nlq.py` 351 项断言把关）。
> 在线形态（方式二）多出的是"数据可实时更新、会话留痕、AI 兜底可用"。

### 方式二：完整工程链路（HDFS + Spark + MySQL + Flask）

```bash
# 1. 安装 Web 依赖
pip install -r requirements.txt

# 2. 启动 HDFS（namenode + datanode）+ Spark 集群 + MySQL
docker compose up -d

# 3. 治理后数据入湖到 HDFS（ODS 原始层，含行为日志导出）
python etl/upload_to_hdfs.py

# 4. 提交数仓全链路作业：ODS → DWD → 空间/类型/科室/网络/时间 五维分析 → ADS 落 MySQL
bash spark/run_all.sh
#   （等价于容器内 spark-submit --packages com.mysql:mysql-connector-j:8.4.0 jobs/run_all.py）
   单独重跑某阶段：bash spark/run_all.sh --only ads  /  --from time；一键全链路：bash scripts/run_pipeline.sh
#   ⚠️ dim_dept_strength 属旧主线遗留阶段（前端已无引用），仅在复查历史数据时才需要重跑

# 5. 重建查询索引（落库以 overwrite 模式写表会 DROP+CREATE，索引会丢失，此步必做）
python etl/create_indexes.py

# 5.5（可选 · 历史遗留）旧导诊知识库维度表——主线筛选不依赖它，仅复查历史数据时执行
python etl/build_disease_dept_map.py     # 生成 data/processed/disease_dept_map.csv
python etl/load_disease_dept.py          # 写入 MySQL 维度表 dim_disease_dept

# 6. 重新生成单文件快照与三份产物（词表由构建脚本从 nlq.py 自动导出，不需要手工同步）
bash web/build_spa.sh

# 7. 构建 Vue 前端（前后端分离形态，产物挂载在 /spa/）
bash web/vue/build.sh

# 8. 启动 Web 服务
bash web/start.sh
# Vue 在线形态   → http://localhost:5001/spa/
# 单文件形态     → http://localhost:5001/
```

HDFS 分层结果可现场核验：

```bash
docker exec hdfs-namenode hdfs dfs -ls -R /hospital | head -30
docker exec hdfs-namenode hdfs dfs -du -s -h /hospital/ods /hospital/dwd /hospital/dws
```

验证服务是否就绪：`curl -s http://127.0.0.1:5001/api/health`
正常返回 `{"institutions":9678,"status":"ok"}`。

- Vue 在线形态（前后端分离）: http://localhost:5001/spa/
- 单文件离线大屏: http://localhost:5001/
- HDFS NameNode UI: http://localhost:9870
- Spark Master UI: http://localhost:8080
- Spark Worker UI: http://localhost:8081
- MySQL: localhost:3307（root/hospital123，应用账号 app/app123）

> Vue 工程说明（技术选型、目录结构、构建与排错）见 [`web/vue/README.md`](web/vue/README.md)。
> 更多运行方式（数据更新链路、常见坑、环境说明）见 [`docs/系统运行流程.md`](docs/系统运行流程.md)。

## API 一览

| 接口 | 说明 |
|---|---|
| `POST /api/nlq/query` | **智能筛选（任务主线核心）**：入参 `q` 中文描述（或直接传 `conditions`）+ 分页与基准点。返回解析出的条件、命中总数、机构清单（含条件匹配度）与 `data_source`（数据来源与口径说明） |
| `GET /api/nlq/parse?q=<中文>` | 只解析不查询：返回条件卡、排序与意图（`filter` / `stats` / `refuse`），供前端即时预览 |
| `GET /api/nlq/ask?q=<中文>` | 规则词表零命中时的兜底通道：由大模型把口语翻译成**结构化条件**（提示词硬约束：只输出条件，禁止机构名与数字） |
| `GET /api/nlq/lexicon` | 词表（行政区 / 等级 / 类型 / 所有制 / 来源 / 科室别名 / 排序 / 统计维度）——单文件形态据此本地复算 |
| `GET /api/institutions` | 七维筛选（区域/等级/类型/科室/协作网络/距离/名称）+ 排序（**条件匹配度/距离/等级/科室收录量/机构名称**，5 种）+ 分页 |
| `GET /api/institutions/<id>` | 机构详情（含科室清单与重点专科标记） |
| `GET /api/overview/districts` / `levels` / `depts` | 区域/等级/科室覆盖概览 |
| `GET /api/specialty` | 重点专科医院列表 |
| `GET /api/meta/filters` | 筛选项可选值 |
| `GET /api/statistics/<dim>` | **维度统计**：`district` / `level` / `category` / `ownership` / `network` 五个维度分组汇总，返回分组、占比、极值组与文字结论 |
| `GET /api/inst/<id>/detail` | **机构详情**：联系方式（地址 / 电话 / 坐标）+ 官方入口（114 平台 / 机构电话 / 高德导航）+ 特色科室（国家 / 市级 / 擅长三级）+ 周边配套（最近地铁站 / 停车场 / 公交站，高德 POI 实时查询） |
| `GET /api/inst/compare?ids=a,b,c` | **多机构横向对比**（2~4 家）：等级 / 类型 / 办别 / 区域 / 重点专科数 / 国家级 / 市级 / 科室数 / 距离（按当前定位或指定起始地址计算）/ 协作网络 / 专科清单 / 地址 / 电话 |
| `GET/POST /api/track` | 行为埋点（`ev`/`k1`/`k2`/`num`），落库 `fact_user_event`；单条 INSERT，失败静默，绝不影响主流程 |
| `GET /api/admin/stats` | **运营统计接口**（页面已下线、接口保留）：① 真实行为统计（热门搜索词 / 热门筛选区域 / 热门导诊病情 / 每日趋势，源自埋点）② 资源热度（区县密度 / 等级构成 / 专科覆盖 / 网络覆盖 / 办别构成） |
| `GET /api/about` | 关于页：数据规模、数仓表清单、Spark 清洗流程、公开数据来源、更新时间与更新日志 |
| `GET /api/ai/health` | 大模型探活（前端据此决定是否开启兜底；`?probe=1` 做真实连通测试） |
| `GET /api/ai/parse?q=<自然语言>` | 大模型把口语解析成筛选条件（规则引擎零命中时的兜底） |
| `GET /api/ai/advice?id=<机构id>` | **AI 机构数据画像**：严格基于该机构真实字段生成一段数据描述；提示词硬约束禁止输出医生姓名 / 职称 / 出诊时间 / 号源数量等未经核实信息（只做数据说明，不做就医建议） |
| `GET /api/ai/warm?queries=a\|b\|c` | 预热大模型缓存（演示/答辩前跑一次，之后同问零等待） |

距离计算采用 Haversine 公式（SQL 内实现）；**条件匹配度**（默认排序口径）= Σ(命中条件权重) ÷ Σ(本次条件权重之和)，权重 行政区 0.30 / 等级 0.25 / 类型 0.20 / 所有制 0.15 / 来源 0.10 —— 它只衡量"与本次条件的吻合程度"，**不是机构实力评分**。床位数因源数据覆盖率仅 0.4% 且取值疑似估算，已从展示中移除。

**智能筛选（一句话 → 条件 → 真实数据）**：`web/nlq.py`（834 行）用词表 + 正则把中文解析成结构化条件（行政区 / 等级 / 类型 / 所有制 / 来源 / 科室 / 关键词），再编译成**参数化 SQL** 查 MySQL `ads_inst_search`；说"按行政区统计"这类说法则切换为维度统计。词表只有一份真源（`web/nlq.py`），由 `etl/export_nlq_lexicon.py` 导出并内联进单文件产物，双端结果由 `etl/verify_offline_nlq.py`（351 项断言）逐字段比对。

**大模型兜底（Agnes AI，可选增强）**：规则词表覆盖不到的口语长句会零命中，此时自动调用 **Agnes**（OpenAI 兼容协议）把这句话翻译成**结构化筛选条件**——提示词硬约束"只输出条件，禁止输出机构名与任何数字"，机构与统计数字一律由 SQL 查询产生。

- 主链路（规则引擎 + 词表）**不依赖任何网络**，答辩断网照常演示；
- 大模型仅在本地零命中时触发，失败/限流一律**优雅降级**，绝不影响主流程；
- 结果按「内存 → MySQL `dim_ai_cache` → 接口」三级缓存，重复提问零额度、零等待（实测 3.6s → 0.05s），且重启服务后依然有效；
- 前端自动探活 `/api/ai/health`：离线单文件（`file://`）与在线静态版**永不发起网络请求**。

配置方式（三级优先）：环境变量 `AI_API_KEY` → 密钥文件 `data/.ai_key.txt`（已被 `.gitignore` 排除，**绝不入库**）。可选覆盖 `AI_API_BASE`（默认 `https://apihub.agnes-ai.com/v1`）、`AI_MODEL`（默认 `agnes-2.0-flash`）、`AI_TIMEOUT`；设 `AI_LLM_ENABLED=0` 可强制关闭，退回纯离线模式。

> **AI 的位置**：只做"中文 → 条件"这一件事，是**筛选助手**而非医疗建议来源；所有机构名与统计数字都来自数据库查询，AI 不产生任何数字。

> 模型选型实测：`agnes-2.0-flash` 平均约 4 秒、答案准确；`agnes-2.5-flash` 平均约 18 秒（最长 32 秒），不适合交互式场景，故默认用前者。
> 免费额度实测约 **1 次 / 30~40 秒**，现场连续提问会触发 429。演示前建议先预热：`bash web/start.sh` 后运行 `python etl/warm_ai_cache.py`（本地知识库已覆盖的问法会自动跳过，不浪费额度）。

**在线演示**：仓库内 `web/dashboard_offline.html` 是零依赖单文件形态（内联数据快照 + 词表 + 筛选引擎，约 10MB），
双击即可使用**完整的智能筛选主线**（自然语言 / 条件双通道、维度统计）与查阅视图，无需启动任何服务。
GitHub Pages 上可直接访问：<https://brooke-boom.github.io/beijing-hospital-spark/web/dashboard_offline.html>
> 该形态的解析与查询由 `web/app.nlq.js` 在浏览器本地完成，与在线接口 `/api/nlq/*` 逐字段一致
> （`etl/verify_offline_nlq.py` 351 项断言把关）。不依赖后端的代价是：数据是构建时的快照、会话留痕只在本地、大模型兜底不可用。

**信息架构：1 条任务主线 + 4 个查阅支撑页**。左侧导航分「主线」与「查阅」两组，浅色/深色统一由设计令牌驱动：

| 层 | 入口 | 内容 |
|---|---|---|
| **主线** | ⭐ **智能筛选** | 双通道：① 说一句话（"朝阳和海淀的三级医院，按距离从近到远"）→ 解析出**可增删的条件卡** → 查询真实数据；② 直接选条件。结果区给命中 KPI、分页清单、地图撒点与「数据来源与口径说明」；说"按行政区统计"则切换为维度统计（图表 + 分组 KPI）；底部保留会话留痕 |
| 查阅 | 🏥 找机构 | 「机构列表」+「条件筛选」两个子标签：七维筛选、勾选对比、分页、机构详情抽屉、自然语言条件转换 |
| 查阅 | 📊 资源画像 | 「总览」+「结构分析」两个子标签：KPI、北京 16 区分级地图（点击联动筛选）、八维分析图表，全部随筛选实时联动 |
| 查阅 | 🗂 数据治理 | 「整合过程」+「质量核验」两个子标签：三类数据源与 Spark 八步流程时间线、关键字段完整率（占主表 / 适用口径双列）、坐标精度分布、去重与多源交叉验证 |
| 查阅 | ℹ️ 系统说明 | 数据规模、公开数据来源、Spark 清洗流程、数仓分层表清单、更新时间与更新日志 |

> 旧路径（`#/institutions`、`#/overview` 等）已做 301 式重定向到对应支撑页，之前分享过的深链接不会失效。
> 数据治理两页所有数字均来自治理脚本真实输出（`data/processed/data_quality_report.md`），由 `build_spa.sh` 注入快照 `overviews.data_quality` 随离线大屏分发，非界面演示数据。

**专科实力指数（Spark 预计算 · 下线主线遗留：作业仍产出，前端已无引用）**：`jobs/dim_dept_strength.py` 把 `national_specialty` / `municipal_specialty` / `feature` / 登记关系四个来源归一到同一套科室字典，取每条(机构×科室)的最高证据等级：

| tier | 标签 | 来源 | MySQL 条数 |
|---|---|---|---|
| 4 | 国家临床重点专科 | `national_specialty` | 227 |
| 3 | 市级重点专科 | `municipal_specialty` | 218 |
| 2 | 官网重点科室 | `feature_level=1` | 266 |
| 1 | 登记重点专科 | `dwd_dept_relation_clean.is_key_specialty` | 2 |
| 0 | 已开设 | 登记关系基础层 | 16,016 |

`strength = 层级基准(80/64/46/28/10) + 机构等级微调(±8) + 协作网络(+4) + 专科集中度(≤8)`，结果落 `ads_dept_strength`（16,729 条 = 9,333 家 × 48 个标准科室）。
其中**专科集中度**= 本专科在该机构全部重点专科中的占比，用来打破"同层级同等级完全并列"——不加它，排序会退化成按机构名拼音排（实测"心慌胸闷"把中医医院排在阜外前面、"孕检"漏掉北京妇产医院）。

**机构详情抽屉**（点击列表任意一行右侧滑出，四个分区页签）：

- **概览**：基本信息、协作网络、**同区同类机构 · 距离对标**（同区 + 同类型 + 同等级的可比机构按距离由近到远，红色为本机构，并给出它在同类中的位次与最近 / 中位 / 最远距离）
- **官方入口与位置**：114 统一平台入口、机构电话、高德一键导航、地址与坐标（系统不抓取也不展示号源与排班）
- **重点专科**：国家级 / 北京市级 / 擅长科室三级清单 + 科室明细表（含是否重点专科与来源）
- **周边配套**：最近地铁站 / 附近停车场 / 附近公交站，来自**高德 POI 实时查询**（含距离、按 75 米/分钟估算的步行时间、独立导航按钮），结果按坐标网格（~100m）缓存进 `dim_poi_cache` 避免重复消耗额度

**机构横向对比**：列表勾选 2~3 家 → 底部浮条 → 对比表。共 16 个对比项，其中数值型（重点专科数 / 国家级 / 市级 / 科室数 / 距离）自动判定"越高越好 / 越近越好"并用绿色标出最优值；距离按当前定位或指定起始地址计算，保证横向可比。

> **关于"名医推荐"的工程取舍**：公开数据中**没有可靠的医生姓名 / 职称 / 出诊信息来源**，凭空生成属于事实性错误，在答辩与实用场景中都会造成误导。因此系统**不收录、不编造任何医生个人信息**，改为把「**真实重点专科清单 → 官方挂号入口**」直接打通：每家机构的特色科室均来自官方公示名单，点击即可跳转 114 平台按机构名检索该科真实出诊专家。`/api/ai/advice` 的提示词也硬性禁止模型输出医生姓名与任何未经核实的具体数字。

**多维分析视图**（全量 9,678 家机构）：办别分布（3,085 / 5,323 / 1,381）、重点专科分级（77 / 166 / 2,111 / 7,435）、协作网络覆盖（7 / 46 / 76 / 8 / 20）、等级 × 办别交叉、专科能力 TOP10（按 `key_specialty_count` 排名）、区域 × 等级堆叠、坐标精度（高精度 / 粗略 / 缺失，直观呈现数据治理成效）、科室覆盖 TOP15（源自 `feature` + `key_depts`）。筛选任一条件（如"三级 + 海淀区"），上述八张图与四项 KPI 立即重算，体现"资源整合后多视角穿透分析"的设计目标。

## 系统架构

```
多源 CSV/XLSX ─治理─▶ data/processed/*.csv ─入湖─▶ HDFS /hospital/ods/raw
                                                        │
                            ┌───────────────────────────┴────────────────────────┐
                            ▼                                                    │
                    Spark 数仓分层（Standalone 集群）                             │
         ODS Parquet ─▶ DWD 清洗关联 ─▶ DWS 五维分析 ─▶ ADS 服务层               │
        /hospital/ods   /hospital/dwd    /hospital/dws    （HDFS 承载①②层，
                                                           结果落 MySQL④）
                            │
              ECharts 可视化 ◀── Flask API ◀── MySQL（ADS 服务层）
                            │
                  build_spa.sh → 单文件离线大屏（快照注入）
```

存储职责划分（对应复盘标准 ①②④）：

| 层 | 存储位置 | 内容 |
|---|---|---|
| 原始层 | HDFS `/hospital/ods/raw` | 治理后 CSV + 行为日志（文本，可回溯） |
| ODS | HDFS `/hospital/ods/parquet` | 原始数据列式化 Parquet |
| DWD | HDFS `/hospital/dwd` | 清洗 + 坐标关联后的明细 |
| DWS | HDFS `/hospital/dws/{space,category,dept,network,time}` | 五个分析维度的汇总结果 |
| ADS | **MySQL** `hospital` 库 | 服务层宽表与分析结果，供 Flask 查询 |
| 元数据 | HDFS `/hospital/meta/snapshots` | 每批次 ETL 快照（时效性维度） |

> 说明：全部数据来自公开渠道清洗整合，无虚构模拟数据。对覆盖率不足或真实性存疑的字段采取"宁缺勿伪"原则——床位数直接移除展示；**科室数**只展示有在线核实来源的 280 家（好大夫在线 + 百度百科；另有 21 家核实值落在六类基层机构上，按其执业形式仍显示"不设科室分科"），8,279 家基层机构（诊所/村卫生室/门诊部/社区卫生服务站/医务室/护理站）按各自《基本标准》以「诊疗科目」执业、本就不设科室建制，如实标注「**不设科室分科**」，其余才标「科室资料待补全」，一律不填推导值；均已在论文中说明。

## 状态

- [x] 数据清洗整合（70 文件 → 9,678 家机构主表）
- [x] **科室字典（32 标准科室 / 11 大类 / 16,438 条映射，重点专科 68 家权威口径）**
- [x] **科室数口径整治（三轮，2026-09-22 定版）**：
  - 第一轮——发现百度百科**模板文字污染**：21 家首医系医院共用同一段词条简介「开放编制床位2500张；共设59个临床、医技科室」，被当成了各家真实的科室数；另有「局内设17个科室，所属机构包括社区矫正中心」实为**司法局**词条串入。新增模板污染检测（同一句证据被 ≥3 家**不同**机构命中即整条作废）+「数字紧邻上下文」口径判据，撤销 33 家污染值。
  - **第二轮（2026-09-20）**——撤掉污染后暴露出更根本的问题：`dept_count` 有 **97% 是推导值**（`source='rule'` 按「等级×类型」套通用清单 11,840 行、`source='name'` 按机构名推导 3,900 行，真实登记行仅 3%）。**1,805 家会重复显示同一个模板数字**（2/6/7/13/19），另有 468 家只登记到 1 条（含东直门医院、东方医院等三甲中医医院）。
  - **第三轮（2026-09-22 联网补全）**——用户反馈"机构列表大部分机构显示『科室资料待补全』"。核实后，这个现象里**混着两件事**，必须拆开治：
    - **真缺数据的**：百科只覆盖 34 家（对一级/二级医院几乎为零）。新增 `etl/scrape_haodf_depts.py` 抓**好大夫在线**北京目录（`/hospital/list-11.html`，435 家，页头直接给出官方口径「N个科室」），`etl/enrich_depts_haodf.py` 做严格匹配（归一化全等 → 去括号 → 别名字典 → **包含式**，且**禁用 `difflib` 相似度**——实测"北京朝阳医院↔北京医院"=0.80 会张冠李戴），产出 294 匹配 / 293 去重 / **267 家回写**。科室明细表因此新增 2,620 条 `source='online'` 的真实科室行（优先级 `specialty > online > key_depts > name > rule`）。合计 **301 家**有在线核实值（好大夫 267 + 百科 34）。
    - **根本不是缺数据的**：另有 **8,279 家（84.6%）** 是诊所 / 村卫生室 / 门诊部 / 社区卫生服务站 / 医务室 / 护理站——这六类按各自《基本标准》以「诊疗科目」核准执业，**本就没有科室建制**。给它们标"待补全"是口径错误，等于把机构的执业形式说成资料缺失。
  - **定版口径（三档穷尽，判据同源）**：① `dept_count_src` 非空**且不属六类** → 显数字（**280 家**）；② 六类基层机构 → 「**不设科室分科**」（**8,279 家**）；③ 其余 → 「科室资料待补全」（**1,230 家**）。⚠️ 判据是**类型优先**：六类基层即便拿到在线核实值也仍显"不设科室分科"——那是它的执业形式，不是资料缺失。三档相加 = 9,678。函数 `deptLabel/deptText`（`web/app.js`）与 `deptKind`（`web/vue/src/deptLabel.js`）判据一致，`etl/smoke_nlq_ui.py` 用**段落正文**（不是 title 前缀）逐格核对三档穷尽、无未归类分支。同步下线 KPI「平均科室数」（改为「科室数已核实」）、排序项改名「科室收录量」、对比表移除该字段的优劣判定、明细表头标注来源（`online` 记「已收录明细」，推导记「推导清单」）。
- [x] Docker Spark 集群搭建与验证
- [x] **HDFS 3.3.6 集群**（自建 arm64 镜像，namenode + datanode，数据入湖 `/hospital/ods/raw`）
- [x] **PySpark 数仓分层（HDFS 承载）：ODS 6 表 → DWD 3 表 → DWS 五个分析维度 → ADS 服务层落 MySQL**
- [x] **分析代码按维度拆分**（`jobs/dim_space|category|dept|network|time.py`，五维独立可跑）
- [x] **Maven 依赖管理**（`spark-submit --packages com.mysql:mysql-connector-j:8.4.0`，替代手工 `--jars`）
- [x] 地理编码（9,675/9,678 = 99.97%，高德 API 断点续跑，缺失 2 家地址不规范）
- [x] Flask 筛选排序 API + ECharts 可视化
- [x] 单文件离线大屏（build_spa.sh 构建链路）
- [x] district 字段治理（41 家空区 → 100% 完整）
- [x] 数据可信度联网验证（与市/区卫健委官方口径横向对比，详见 docs/毕业论文.md 5.3.1）
- [x] 国家临床重点专科联网增强（43 家医院 / 229 条，新增 national_specialty 字段并在详情浮层展示）
- [x] feature 字段反向补全（17 家 / +78 条，ads_specialty_hospital 418 → 496 条，两套专科口径自洽）
- [x] 市/区级临床重点专科联网增强（**77 家医院 / 283 条**，新增 municipal_specialty 字段并在详情浮层展示；中医"十四五"首批 74 项 + 第二批 107 项全部抓齐）
- [x] 三层专科口径自洽（feature 擅长分级 → national_specialty 国家级 → municipal_specialty 市/区级，50 家 municipal_only 补足先前无国家级认定的三级医院）
- [x] **自然语言智能筛选（当前主线）**（`web/nlq.py` 834 行规则引擎 + 双通道输入 + 五个维度统计 + 医疗边界硬拒答；单文件形态由 `web/app.nlq.js` 本地复算，双端 351 项一致性断言）
- [x] **系统定位整改**（2026-09-20：「就医决策 / 智能导诊」下线，主线收敛为"资源数据查询与筛选"；`/api/plan`、`/api/triage`、`user_plan` 表与 `web/app.plan*.js` 全部移除）
- [x] **可视化大屏导航栏 + 多维分析视图**（左侧七视图导航：数据总览 / 医疗资源分析 / 机构查询 / 智能筛选 / 数据整合 / 数据质量 / 系统说明；医疗资源分析**六个 KPI + 15 张图表分四段**（资源结构 / 专科能力 / 空间格局 / 协作与数据整合），全部随七维筛选实时联动，ECharts 渲染）
- [x] **医疗资源分析页扩充（2026-09-21）**：新增 7 个分析维度——机构类型×办别结构、专科能力分档、国家级/市级重点专科对比、区域专科实力、区域资源禀赋矩阵（16 区 × 5 维 · 列内归一 · 格内印原值）、协作网络×等级、数据整合深度（按合并源文件数分档）；新增三项交互——**点图下钻**（类型/区域/等级/网络/专科五类图元素点击即回总览页按该维度筛选，不可筛的维度如实说明而非假跳转）、专科 TOP 档位切换（10/15/20）、**导出分析数据 CSV**（7 张聚合表）与**复制分析摘要**；口径不可比的维度一律带 tooltip 说明来路
- [x] **大屏布局缺陷修复（2026-09-21）**：两处「不报错、但看得见」的问题——
  - ① **左侧导航跟随页面滚动**：原先用 `position:sticky`，而 sticky 的可停留范围 = 包含块高度 − 元素高度；数据质量页这类内容只比一屏高一丁点的页面里，这个范围只剩约 90px，滚过就带着侧栏一起往上跑（顶部「智能筛选」入口被推出视口）。改为 `position:fixed` + 主列 `margin-left:var(--sb-w)`，窄屏只需改 `--sb-w` 一个变量即可让侧栏宽度与主列让位同步收窄。
  - ② **数据质量页表格面板内容溢出**：`.apanel` 固定 `height:314px` 是为了给 `.achart` 里的 ECharts 一个确定高度（canvas 拿不到高度就是 0 → 白板），但表格行数随数据变化——「关键字段完整率」超出 146px、「机构类别构成」超出 157px，末尾几行直接画到面板外、叠在页脚上。新增 `.apanel.grow`（按内容撑开 + `min-height:314px` 保住视觉齐平）用于该页三个表格面板；图表面板仍保持定高。
  - 顺带修：切主题时数据质量页的坐标精度饼图不重绘（`applyTheme` 漏调 `renderQuality`），浅色主题下会留着深色配色。
- [x] **自动定位越界误判修复（2026-09-21）**：在北京市以外的城市（如南昌）打开页面，会被贴上「北京市大兴区」的假区名——
  - 根因：`locateHint()` 取「离坐标最近的机构」的 `district` 当区名，而**最近机构永远存在**、没有任何范围约束；南昌到大兴直线约 1,200 km，大兴是北京最南端的区，于是从南方来的点全落在它头上。最误导的是**标签假、距离真**：页面同时显示「我的位置 · 北京市大兴区(±435m)」与「1175.9 km」，看起来像距离算错了。
  - 修复：新增域内判定（到最近机构 > **60 km** 即视为域外）。**自动定位落到域外时不接管基准**（保留默认「天安门」，否则整页按 1,000+ km 排序毫无参考意义）并说明原因；**手动点「定位」仍尊重用户意图**，但标签如实写「我的位置 · 市域外 · ±435m」，不再臆造区名。另清理 localStorage 中旧版写入的脏基准（名字带「北京市」而坐标在域外 → 丢弃）。
  - 验证：12 点坐标矩阵（南昌/上海/广州/乌鲁木齐 → 域外；大兴/延庆/密云/平谷/房山/东城 → 域内，距最近机构均 <2 km）；真浏览器 geolocation 三场景截图；冒烟断言 19 → **22 项**，反向测试（禁用域外分支）三条立即报红。
- [x] **大屏响应式版面美化（2026-09-21）**：四个页面「看着杂乱」的观感问题逐条量化收口——
  - ① **医疗资源分析页分段标题不醒目**：`.sec-title` 由 14px/400 的灰字升为 **15.5px/700**，并加主色渐变竖条 + 等宽序号（`01~04`）+ 整行发丝分隔线 + 线左端 64px 主色短横，四段之间的层级一眼可辨。
  - ② **数据总览页单调、地图过大**：右栏 .panel 包进 `.panel-stack` 与地图**同高同 sticky**（实测 808 ↔ 808 齐平），两张小图在右栏内部 `flex:1 1 0` 等分；KPI 行接回项目早已定义却未启用的 `.kpis.k4`，由 6 列栅格塞 4 卡（右侧空 1/3）改为 4 列铺满。
  - ③ **数据整合页三块画布排列杂乱**：由 3 列不等宽改为 **2 列等宽**（实测 590/590，左右底端差 0）——左栏上下叠「源文件 / 整合结果」、右栏「Spark 管道」跨两行、ETL 明细表 `.integ-full` 独占整行；并给 8 列宽表加 `min-width:0;overflow-x:auto`，防其在窄轨道里撑破网格。
  - ④ **数据质量页分布杂乱**：KPI 同样接 `k4` 四列铺满；栅格改 **6 列制**（`.agrid` 每格 `span 2` + `ag-n3/ag-n4/ag-n5` 变体），杜绝「末行 3+1 甩一个孤块」；「字段完整率」2×2 与其余面板在行内 `stretch` 等高。
  - 新增闸门 **`etl/verify_layout.py`（75 项断言）**：Playwright 显式视口 × 5 档宽度（1440/1280/1200/1080/900）× 4 个页面，量横向溢出、内容逃逸（`overflow:visible` 却比容器大）、行内参差、末行填充率、每行最少格数、行内等宽、整合页两栏齐平、质量页 2×2、标题字号字重与分隔线（此时 75 项；第 ⑥ 类判据见下一条，已扩到 81 项）。反向测试 R1（面板恢复定高）/ R2（网格退化成一格一行）/ R3（删掉窄屏定位重置）三个样本全部被捕获。
  - 踩坑留档：**显式网格定位 + 会在窄屏塌成单列的断点，塌陷处必须成对重置 `grid-column/grid-row`**——否则 `grid-column:2` 在只有 1 条显式轨道时会**创建一条隐式列**，1080px 下实测隐式列吃掉 936px、真正的 1fr 列被挤到 36px。
- [x] **总览页「办别构成」空白修复（2026-09-21）**：面板在、标题在、**数据也全在**（探针查到 `series` 里 3 个点：公立 3,085 / 民营 5,323 / 未标注 1,381），只是画在一张 0 像素高的画布上——
  - 根因：图表容器拿高度只有三条路 —— ①列进 `#ch1,#ch2,…{width:100%;height:100%}` 的 id 清单（`.panel.sm` 给了固定 262px，`height:100%` 才解得开）②带 `.achart` 类（`.achart{flex:1}`）③`#map` 自己的 `flex:1`。**`#ch_ov_own` 三条都不是**，它的 id 从没写进清单。全项目 33 个容器目标里只有它漏登记，所以只有这一个面板是白的。
  - 关键点：ECharts 在容器高 0 时**不报错、不警告**——照样 `init`、照样 `setOption`、`series` 里数据齐全，只是什么都看不见。这是最容易被误读成「这个维度没数据」的故障形态。
  - 修法：补进 id 清单，并在清单上方写清「**新增图表必须同时改三处**：高度清单 / `resizeAll()` 数组 / 空态↔有数据的 `setOption(opt, true)`」。
  - 闸门加固：`etl/verify_layout.py` 新增第 ⑤ 类判据「空板」（68 → **75 项**）——静态查「凡 `echarts.init` 的 id 必有高度来源」（三种 init 写法都认，含最易漏的 `const qc = $('ch_qcoord'); echarts.init(qc)`）+ 反向查「HTML 里的 `ch*` 容器必有 JS init」；运行时在 5 档宽度下逐视图量 `clientWidth/clientHeight`。反向测试 R4（删掉清单里的 id）报红 6 项、R5（只改 HTML 的 id 名）报红 2 项。
- [x] **分析页可视化形态多元化（2026-09-21）**：用户反馈「医疗资源整合部分都是条形图」——实测确实如此：15 张图里 **12 张是条形系**（横条 6 / 柱状 3 / 堆叠柱 3），只剩 2 饼 + 1 雷达。本轮按**图形语义**逐张换形（不是为换而换）：
  （那只雷达后来也因子指标口径不可比，被换成「区域资源禀赋矩阵」，见 2026-09-22 条目。）
  - **等级 × 办别 → 旭日图**（内环等级 / 外环办别，面积 ∝ 机构数）；口径限定在**参评机构**——全量 8,804 家「不适用医院分级」若入场会占掉 90% 圆环、把三/二/一级压成细线，故图上与提示里都写明「不含不适用 X 家」。
  - **机构类型 × 办别 → 矩形树图**（外块 = 类型规模、块内按办别切分）：比原来的 100% 堆叠条多表达了「哪类机构多」这一层。
  - **重点专科分级 → 棒棒糖图**：7,435 / 2,111 / 166 / 77 是断崖式分布，面积类图形会被最大项吃掉，所以保留长度编码、只换形态。
  - **专科能力分档 → 帕累托图**（柱 = 各档机构数 + 线 = 累计占比），补上「头部两档就吃掉 90.9%」这层信息。
  - **专科级别对比 → 桑基图**（机构等级 → 国家级 / 市级）：把「国家级专科几乎全挂在三级机构上」变成可见的流向。
  - **区域 × 等级 → 热力图**（16 区 × 4 个参评等级）：原来 16 根堆叠柱要靠读柱段比高低。按**非零值分位数**分档而非连续色带——同一色带上最大格与最小格差两个数量级，连续色带会把小区压成一片淡色；格内同时印原始数字，颜色看格局、数字读精确值。**行序按三级机构数降序**（见下条，2026-09-22 修正）。
  - **数据整合深度 → 漏斗图**（来源越多机构越少，末档 0.7% 靠 `minSize` 保住可点性）；**坐标精度 → 仪表盘**（单一比率指标，读「离满分还有多远」比三段饼更贴切）。
  - 保留条形的只有四张**榜单 / 排行**（专科能力 TOP、科室覆盖 TOP15、区域专科实力、协作网络规模）——排序数据本就该用条形。
  - 顺带挖出一个**早于本轮就存在的隐性 bug**：`OWN_SERIES` 是模块级 `const`，而它引用的 `ACC2/WARN/DIM` 要到 `loadTokens()` 才赋值 → 三个颜色全是 `null`，ECharts **静默回落到自带默认调色板**（"办别配色"其实一直是七彩、与页面别处不一致，且不报错）。改为函数按调用时取值。
  - 闸门 `etl/verify_layout.py` 新增第 ⑥ 类判据（75 → **81 项**）：静态查「分析页图形形态覆盖必备集合」+「模块级常量没有捕获主题令牌」；并在**真 ECharts 页**（非空桩）上运行「15 张图均已绘制 / 图形类型符合预期 / 形态 ≥10 种 / 办别三色与主题令牌一致」。反向测试 R6（桑基改回条形）/ R7（用常量捕获令牌）/ R8（树图叶子改用默认调色板色）三个样本全部被捕获。
  - 点击下钻按新图形全部重测：旭日（内环→等级、外环→等级并带出办别）、树图→机构类型、桑基→等级（右侧节点是专科级别，如实提示不假跳转）、热力图→**区 + 等级**双条件下钻。
- [x] **分析页可视化形态回调：可用性优先于形态多样性（2026-09-22）**：用户看大屏后反馈「不够直观，像为了多样化而多样化，我的目的是使系统可用性更高」。复盘确认——上一轮为凑形态换的那 5 张，恰恰是最难读的 5 张，逐张改回条形系：
  - **等级 × 办别：旭日图 → 纵向堆叠条形**。原旭日内环 4 等级 × 外环 3 办别 = 12 个扇区，摊到 1,289 家参评机构后多格只有个位数、外环宽度不到 8px，白字标签压在同色浅块上根本读不出来（截图为证）。改后：柱高 = 该等级机构数（规模），段宽 = 办别构成（结构），段内直接印机构数 —— 一次读完两个问题，不必 hover。
  - **机构类型 × 办别：矩形树图 → 横向堆叠条形**。树图用**面积**编码机构数，而人对面积的判断精度远低于长度（同为 500 家，「又宽又扁」与「接近正方」看起来差很多）；小类型被压成窄带、块内标签只能截断成「卫生服务…」，右下角一列小块完全无法辨认。改后条长严格等比，10 行长度可直接跨行对齐比较。
  - **重点专科分级：棒棒糖 → 水平条形**。7,435 / 2,111 / 166 / 77 是断崖分布，棒棒糖靠圆点定位，166 与 77 两点紧贴纵轴、实测几乎重叠；横轴刻度也被迫挤成 `1,0002,0003,000…`。改后末端直接印数值，小值也读得准。
  - **专科级别对比：桑基图 → 100% 堆叠条形**。桑基的语义是「多对多流向」，而这里只有 4 个机构等级 → 2 个专科级别，右侧永远只有两个节点；实测整块面积被「三级」一条流带占满、下方大片空白，有效信息量等于一张 4×2 交叉表。
  - **数据整合深度：漏斗图 → 水平条形**。原漏斗为保住末档可点性设了 `minSize: '22%'`，把 0.7% 的末档画成 22% 宽 —— 宽度与实际占比脱钩；而这张图要说的「来源越多、机构越少」恰恰只能靠宽度传达，等于交了一张失真的图。改后长度严格等比，机构数与占比都写在末端。
  - **保留**：环形（办别构成）、帕累托（柱 + 累计占比线）、热力图（16 区 × 4 参评等级矩阵，按三级机构数降序排）、仪表盘（单比率达成度）、连续色带矩阵（16 区 × 5 维资源禀赋，列内归一），以及四张**榜单/排行**条形（专科能力 TOP、科室覆盖 TOP15、区域专科实力、协作网络规模）。
  - 顺带修掉两个「条形化之后才暴露」的问题：① 窄面板（约 356px）上 value 轴拉到 7,000 量级时刻度叠印成一团 → 全局轴样式加 `axisLabel.hideOverlap` 与 `splitNumber: 4`；② 机构类型截断到 6 字后「社区卫生服务站 / 社区卫生服务中心」双双变成「社区卫生服务」而重名 → 截断放宽到 8 字并加宽轴位。
  - **闸门判据同步改向**：`etl/verify_layout.py` 第 ⑥ 类从「形态 ≥11 种 + 必备集合含旭日/树图/桑基/漏斗」改为「**逐图形态钉死** + bar 之外仍须覆盖 `{pie, line, heatmap, gauge}` 四类编码 + 形态 ≥5 种」——不再数形态个数冲指标，改为盯「每张图用它最擅长的形态」与「不许退化成纯条形」。断言总数仍 **81 项**全绿。同时 `colors()` 探针补上 **series 级配色**读取：堆叠条形的办别色挂在 `series.itemStyle` 上，旧实现只读 `data[].itemStyle`，改形态后会静默返回空对象、让这条断言空转。
  - 下钻按新形态重测 5 条路径全部通过：等级×办别→等级（三级 213 家）、类型×办别→机构类型（医院 732 / 诊所 3,211）、专科级别分布→等级（二级 212 / 三级 213）。
- [x] **两张「办别构成」图重做：自筛选豁免 + 100% 构成条（2026-09-22）**：用户追问这两张图「的意义」并指出「太丑、未达到可视化的目的」。对着数据核完，发现是三个问题叠在一起：
  - **① 图的维度 == 筛选维度 ⇒ 必然塌成单柱**。截图那一刻「等级 = 三级」是生效的（三级×办别 = 113/16/84、三级×类型 = 医院 196 + 妇幼保健 9 + …，与图逐项吻合），于是「等级 × 办别」只剩三级一行 —— 这张图不再提供任何信息，只是把用户刚设的条件又念了一遍。修法：新增 `anSubset(except)` 做**自筛选豁免**，两张图各自忽略自己那一维的筛选条件，始终展示完整分组，并把当前筛选那一行**加粗**标出。副产物：可以直接在图上点另一行换筛选，不必先清条件 —— 下钻实测 5 条全过，含「已筛三级 → 点一级 → 切到一级 505 家」。
  - **② 类型间量级差 115 倍，长度编码不可读**。诊所 3,211 家 vs 体检中心 28 家，线性轴上后几名只有 1~3px，就是几条「发丝」。修法：两张图统一改为 **100% 构成条**（段长 = 办别占比），行标签写「名称 · N 家」补回被归一化丢掉的量级 —— 每行等长后跨行只看段长，不再受规模干扰。
  - **③ 一个只有截图才能发现的真 bug**：第一版只把「段内标签」换成了百分比、`data` 仍送**原始家数**，而 x 轴是 0~100 —— 833 家在轴上直接顶满、31 家只有 31% 宽。**标签数字对、条长全错**，探针也查不出来（`series` 结构完全合法）。修法：`data` 改送 `own / n * 100`，并把这条教训写进注释。
  - 另修三处细节：**轴序反转**（ECharts 横向条形把数组第 1 项画在最下面，原来「未定级」顶在最上、「三级」沉在最底，与等级由高到低的读序相反）；**刻度不等距**（`interval` 误挂到 `axisLabel` 里，而它是类目轴选项、在值轴上被静默忽略 → 刻度成 0/30/60/90 且丢掉 100%；移到轴级后固定为 0/25/50/75/100）；**低样本假精确**（筛到三级后出现「急救中心 · 1 家 → 100%」，1 家机构的 100% 是假精确 → `n < 5` 的行段内改印家数）。
  - 三色（公立 / 民营 / 未标注）不变；`verify_layout` 81 项、`smoke_dashboard`、`verify_artifacts`、`verify_offline_nlq` 351 项全绿。
- [x] **「区域 × 等级」矩阵的假热格：等级适用范围治理（2026-09-22）**：用户看 03 段「空间格局」后问「这部分可视化或者数据是否有问题」。查出来**数据真的有问题**，而且是两层叠在一起：
  - **现象**：矩阵里**大兴区「一级」= 369 家**，是全表最大的格，行序按行合计排序时它直接被顶到最顶行，视觉上像全市资源第一区 —— 而大兴区三级机构只有 11 家（海淀 37）。继续查：全表 833 家「一级」里大兴区独占 369 家，且**全国所有「一级诊所」(175) 与「一级医务室」(87) 全部集中在大兴区**。真实分布不可能长这样。
  - **根因①（文件名误解析）**：`etl/clean_merge.py` 的 `std_level()` 在单元格无等级时用**源文件名兜底**，正则是 `(三级甲等|…|一级|二级|三级)`。而源文件《**大兴区一级以下医院名单**门诊部名单、诊所名单、医务室名单.csv》里那个"一级"被"以下"修饰，这份名单实际**跨等级**（一级＋未定级），旧逻辑让整份 318 行无条件盖上「一级」（其中 313 家是诊所/医务室/门诊部）；《丰台区**村一级**卫生室.csv》里的"一级"其实是"村级卫生室"的意思，又让 16 家村卫生室变成「一级」。→ 改为：等级词前一字为「村」、或被「以下/以上/以内」修饰时**放弃文件名兜底**。
  - **根因②（缺适用范围守卫）**：三级/二级/一级是《医疗机构管理条例》下对**医院**的评审结论。诊所、村卫生室、门诊部、社区卫生服务站、医务室、护理站六类按各自《基本标准》以「诊疗科目」核准执业，**本就没有等级建制**，其 level 只可能来自脏单元格或根因①。→ 在 `clean_merge.py` 立判据函数 `level_not_applicable()`（模块级，供复用），对这批数据做一次性外科式修复 `etl/fix_level_not_applicable.py`（先备份、留 `level_src` 清除原因、出治理报告），并**同时**把 `grade_scope` 归位为 `not_applicable` —— 因为 `spark/jobs/layer_dwd.py` 是拿 `grade_scope` 决定 `level_norm='不适用医院分级'` 的，只清 level 而不改 scope，这 334 家仍会被当"参评机构"以「未定级」占位，等于没修。`etl/build_grade_scope.py` 的顺序缺陷（「level 非空 → applicable」短路在前，导致被误赋等级的机构反被判成"参评"）一并修正。
  - **结果**：清除 **335** 家（大兴 318 / 丰台 17），大兴区「一级」369 → **51**，全表「一级」833 → 498；六类基层机构挂医院等级数 = **0**，`grade_scope` 由 `applicable 1,289` 收敛到 `955`（当日稀疏字段补全后为 `967`，见「稀疏字段三路补全」条目）。判据与前端「不设科室分科」的 `NO_DEPT_CAT` 同源，**全项目只认一套口径**。
  - **可视化侧同步修**：① **行序**从「行合计降序」改为「三级 → 二级 → 一级字典序降序」——按行合计排等于把"基层机构多"读成"资源强"，即便数据修完，社区卫生服务中心多的区仍会排在前面；改后顶部是海淀 37 / 西城 34 / 朝阳 30，与隔壁「区域专科实力」图口径一致。② 分区提示补上**来源披露**：等级取自各区上报的医院名单，**各区收录完备度不一**（大兴有《一级医院名单》可整份枚举，朝阳没有对应文件），跨区比「一级」家数会被覆盖度带偏。
  - **闸门加固**：`etl/verify_offline_nlq.py` 新增 2 项真库断言 ——「基层六类不得挂一/二/三级」与「基层六类 `grade_scope` 必须为 `not_applicable`」（349 → **351 项**），否则下次重跑清洗又会滑回去。
  - **一处连带改善**：等级修正后 `rule_depts()` 的落点也变了一致 —— 此前同一类**诊所**在大兴区（有假等级）拿 `RULES['一级']` 的 6 条科室、在朝阳区（无等级）走 `["普内科"]` 兜底，**跨区清单不同**；现在统一走兜底。`hospital_depts` 16,893 → 16,438 行。
- [x] **v5.0 信息架构重构**（2026-09-18：导航重构为七视图，与论文题目一一对应；新增机构查询 / 智能筛选 / 数据整合 / 数据质量四个独立页面）
- [x] **机构详情抽屉**（概览 / 就诊与挂号 / 重点专科 / 周边配套 四个分区页签；周边配套接入高德 POI 实时查询并按坐标网格缓存至 `dim_poi_cache`）
- [x] **机构横向对比**（勾选 2~3 家 → 16 项对比表，数值项自动判定优劣并高亮最优值，距离按当前定位或指定起始地址计算）
- [x] **行为埋点与运营统计**（`fact_user_event` 真实行为埋点 + 资源热度双板块；`/api/track` 单条 INSERT 失败静默，绝不影响主流程；独立运营后台页在 v5.0 重构中下线，`/api/admin/stats` 接口保留）
- [x] **关于页**（数据来源、Spark 七步清洗流程、数仓分层表清单、更新时间与更新日志，全部取自真实表）

- [x] **稀疏字段三路补全 + 完整率口径纠偏**（2026-09-22 深夜）：用户看「数据质量 → 关键字段完整率」后问「红色框里的字段能不能联网补充完善」。查下来的结论是**主因不是公开数据没有，而是合并阶段把列丢了** —— 70 个源文件里本来就有电话（`北京市健康体检机构信息.csv` 209 家）、等级（`医疗保险定点医疗机构名单.csv` 230 家）、等次（`西城区医院基础信息.csv` 20 家），`clean_merge.py` 按固定字段白名单取列，这些列从未进主表。
  - **三路补全，先离线后联网**：① `etl/harvest_source_fields.py` 回收源文件列（不联网、不推断、不估算；机构名匹配用 3-gram 倒排索引做严格包含式，**禁用 difflib 相似度**——"北京朝阳医院"vs"北京医院"相似度 0.80，必错配）；② `etl/harvest_baike_fields.py` 复用此前抓了但没取用的百科 infobox 等级（**零新增请求**补出 80 条等次）；③ `etl/scrape_haodf_contacts.py` 联网逐家核实 435 家医院详情页（403 家电话 / 180 家等级，排除"下设/托管某院"这类别家描述）。三路经 `etl/apply_field_patch.py` 合并回写，**只填空、不覆盖、冲突整条丢弃、不写占位值**。
  - **结果**：电话 570 → **823（8.4%）**、等级 922 → **849**（应参评 874 家，适用口径补全率 **97.1%**）、等次 175 → **255**；冲突 29 条（主表三级 / 医保名单二级之类）留档不自动改，越界 4 条（落在门诊部 / 社区卫生服务站上的等级值）被等级适用范围判据拦下。交通导引 17 / 机构官网 15 确认公开渠道无系统出处，**如实保留为空**。
  - **⚠️ 顺带查出一个口径错误**：`web/build_spa.sh` 自己维护了一份比 `etl/build_grade_scope.py` 更窄的「不设等级建制」类别名单，漏掉社区卫生服务中心 / 急救机构 / 公共卫生机构 / 体检机构 / 其他医疗机构五类，等级完整率的适用分母因此从 **967 虚增到 1,510**，页面显示成「62.4%」——而真实值是 97.1%。已改为直接读主表 `grade_scope` 列：**同一口径在 `build_grade_scope.py` / `clean_merge.py` / DWD `level_norm` / 前端 `dqLocal()` 四处共用，不在别处二次判定**。`grade_scope` 随之重算：应参评 955 → **967**（12 家有等级记录的机构从 not_applicable 归位），未定级 33 → **25**。
  - **数字同步脚本化**：口径一变，同一组数字散落在 11 个文档与前端注释里，手工 grep 替换必漏；漏一份的后果不是"不好看"，而是答辩时论文说 498、系统说 505，当场自相矛盾。新增 `etl/sync_docs_metrics.py` —— 字面量精确匹配（匹配不到**直接报错退出**，绝不做模糊替换）、默认 dry-run、跑完再扫一遍残留旧值。本轮同步 README / 毕业论文 / 项目说明书 / 功能点-代码对应表 / 演示指南 / 答辩手册 / 问题库 / 数据联网核实报告 / 项目最终整改报告 共 13 个文件（README + `docs/` 10 份 + `web/app.js` + `web/dashboard.html`）。
  - 闸门：`verify_offline_nlq` **351 项**、`verify_layout` **81 项**、`verify_artifacts`、`smoke_dashboard` / `smoke_vue` / `smoke_nlq_ui` 全绿；全链路（入湖 → 数仓 → 落库 → 索引 → 前端产物 → 校验）单机重跑 **3 分 56 秒**。
- [x] **图表「口径条」：让每张图自己交代"我在读哪一份数据"**（2026-09-22 深夜）：用户对首页「医疗机构类型构成」截图提问「这里是不是有缺失数据」。核完确认**数据没丢**：该图读的是筛选后的结果集（当时「等级 = 三级」生效，只剩 212 家），而旁边 KPI「医院 732 家」读的是全量口径 —— 两个数字都真，但**没有任何界面元素提示它们口径不同**，用户只能读出"数据缺失"。
  - **根因是设计缺陷，不是数据缺陷**：总览页的图与 KPI 分别绑 `FILTERED` 与全量两种数据源，而页面只提供筛选 chip，没有一处常驻说明当前口径、也没有一键回到全量的入口。
  - **修法**：新增 `scopeText() / paintScopes() / activeConds()`，在 `updateCharts()` 首行给 `ch1`（等级分布）`ch2`（类型构成）`ch3`（区域分布）与地图挂**常驻口径条**——无筛选时声明静态口径（如「10 类归一口径」「等级只对参加评审的机构有意义」），有筛选时切警示色、写明生效条件并给出**一键清除筛选**按钮。口径变化时 `setOption(opt, true)` 整体重绘，避免旧 series 残留。
  - 附带把「关键字段完整率」改成 **`dqLocal()` 现算**：指标从当前加载的快照行直接统计，与列表读同一个数组，列表变它就变，彻底消灭"写死兜底常量"这一类数字漂移。### 待决策（已定位、未动手；一旦改动会牵动答辩口径）

| 事项 | 现状 | 为什么没顺手改 |
|---|---|---|
| **主表机构去重漏 2 组** | `中国中医研究院广安门医院` 与 `中国中医科学院广安门医院`（旧名 / 新名）、`北京儿童医院` 与 `首都医科大学附属北京儿童医院`（简称 / 全称）各存两条。两组**都在在线核实值覆盖范围内**（该范围现为 301 家），于是同一家医院在列表中出现两次相同科室数——「35 个」看似出现 4~5 次，实际只有 3 家医院（广安门、北京电力医院、北京儿童医院） | 去重会使机构总数从 **9,678** 变动，而该数字已写入论文、PPT 与全部答辩材料 |
| **默认排序含科室权重** | `web/app.js` 的 `W_LEVEL 0.5 / W_DIST 0.3 / W_DEPT 0.2`，而 `dept_count` 80.3% 是推导值——结果是机构查询页默认列表顶部**总是**那几家有在线核实值的机构（301 / 同仁 / 第五医学中心） | 该权重已写入论文与 PPT 的排序口径说明 |
| **Vue 形态的分析页未同步扩充** | 单文件大屏的医疗资源分析页已是 6 KPI + 15 图 + 下钻交互；Vue 形态（资源画像 → 结构分析）仍是原有的 4 图 + 明细表 | 它只拿得到 `overviews` 聚合表（districts / categories / levels / networks），**没有机构明细**，类型×办别、专科分档、整合深度这类交叉维度需要新增 2–3 个聚合接口才能算，属独立改动 |

> 两项均在本轮如实记录而非掩盖（详见 `docs/开发日志.md` 2026-09-20 章节第七节）。

> **数据可信度**（2026-09-10 联网核实）：以北京市医保 A 类定点医疗机构名单（2026-08-08，58 项）为基准交叉验证，本系统**覆盖率 100%、等级标注正确率 100%**；主表 9,678 家与官方全量口径的差额主要为基层机构（村卫生室/社区卫生服务站）覆盖度差异，属统计口径不同而非数据错误，详见 `docs/数据联网核实报告.md`。当前等级分布（应参评 874 家）：一级 480 / 二级 189 / 三级 180 / 未定级 25；另有 8,804 家「不适用医院分级」。（2026-09-22 修正：原「一级 498」含 335 家被源文件名误盖等级的基层机构，见下方等级适用范围治理条目。）
