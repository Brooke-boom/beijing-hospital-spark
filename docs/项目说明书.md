# 项目说明书

**项目名称**：基于Spark的北京市医疗机构资源整合与多维筛选可视化系统
**作者**：刘佳鑫（2025101020002）· 江西软件职业技术大学 软件工程学院
**指导教师**：万泽云
**代码仓库**：`Brooke-boom/beijing-hospital-spark`
**文档版本**：v1.0（2026-09-18）
**适用范围**：毕业设计验收、答辩答疑、项目交接

---

## 一、项目概述

### 1.1 背景

北京市医疗机构信息公开程度较高，但分散在市医保局、各区卫健委、各类专科公示名单等**70 个异构数据文件**中，格式、字段、命名口径各不相同（CSV/GBK/伪装成 CSV 的 xlsx/老 xls 混杂）。普通用户想要回答"我家附近哪家医院有儿科、而且是三级、并且属于儿科医联体"，需要同时打开十几个网页逐条比对。

本项目要解决的不是"再做一个漂亮的大屏"，而是**先把 70 份文件真正整合成一份可信的数据资产，再在这份资产上提供多维筛选与可视化**。

### 1.2 目标

| 目标 | 度量 |
|---|---|
| 数据整合 | 70 个源文件 → 1 份主表（9,789 家机构 × 42 列），机构名+区县级别去重 |
| 数据可信 | 坐标覆盖 99.98%；办别未标注率从 15.5% 降至 14.1%；等级适用范围可解释 |
| 大数据链路真实 | 数据从 HDFS 读、预处理结果回写 HDFS、分析结果落 MySQL，全链路可一键重跑 |
| 多维筛选 | 7 个维度（关键词/区域/等级/类型/科室/协作网络/距离）任意组合 + 4 种排序 |
| 可视化 | 7 个视图、16 区地图撒点、图表与列表联动互指 |

### 1.3 成果清单

| 类别 | 成果 |
|---|---|
| 数据资产 | `data/processed/master_institutions.csv`（9,789 × 42）及 6 份衍生数据表 |
| 数据治理代码 | `etl/` 56 个 Python 脚本（核心管线 23 个） |
| 数仓代码 | `spark/jobs/` 10 个模块，共 1,073 行 |
| 服务端 | `web/app.py` 1,651 行，22 个 REST 路由 |
| 前端（在线） | `web/vue/`（Vue 3 工程：7 视图 + 6 组件 + api/store/charts），构建产物由 Flask 挂载在 `/spa/` |
| 前端（离线） | `web/dashboard.html` + `web/app.js`（3,314 行），派生出 3 份可交付 HTML |
| 部署 | `docker-compose.yml` 5 容器（HDFS ×2 / Spark ×2 / MySQL ×1） |
| 文档 | 本说明书、《功能点-代码对应表》《系统运行流程》《毕业论文》《答辩与简历讲解手册》等 |

---

## 二、需求与范围

### 2.1 功能需求

| 编号 | 需求 | 实现位置 |
|---|---|---|
| F1 | 多源医疗机构数据整合为统一主表 | `etl/clean_merge.py`、`etl/govern_master.py` |
| F2 | 七维筛选（关键词/区域/等级/类型/科室/协作网络/距离） | `web/app.py` `/api/institutions`、`web/app.js` `applyFilter` |
| F3 | 多种排序（综合评分/等级/距离/机构名） | `web/app.js` `curSort` / `setSort` |
| F4 | 16 区地图撒点与筛选联动 | `web/app.js` `drawMap` |
| F5 | 机构详情（联系方式/特色科室/周边配套） | `/api/inst/<id>/detail`、`paintDrawerFull` |
| F6 | 2–4 家机构横向对比 | `/api/inst/compare`、`paintCompare` |
| F7 | 自然语言筛选（"朝阳区的三级医院"） | `parseNLQ` / `commitNLQ` |
| F8 | 智能导诊（症状 → 科室 → 医院） | `/api/triage`、`requestTriage` |
| F9 | 数据来源、清洗流程与数据质量可查证 | 数据整合页 / 数据质量页 |
| F10 | 地址定位与距离计算 | `/api/geocode`、`resolveBase`、`haversine` |

### 2.2 非功能需求

- **离线可用**：`dashboard_offline.html` 双击即开，零依赖、零网络请求（演示抗风险）。
- **可重跑**：从入库到落库全链路一键重跑，无需手工补数据。
- **可验证**：提供三条自检命令，现场即可证明链路真实（见第九章）。
- **不伪造**：任何指标都能对应到具体代码与数据行；数据缺失如实标注，不补零、不臆造。

### 2.3 范围边界（明确不做的部分）

- **不做实时数据**：数据为公开静态名录，快照日期即数据截止日，不做实时同步。
- **不做医疗建议**：智能导诊只输出"具备相应科室能力的机构推荐"，不给诊断意见。
- **不做无依据的推荐**：系统是"资源整合 + 多维筛选 + 排序"，不构建用户画像、不做个性化推荐算法。

---

## 三、技术选型与论证

### 3.1 选型总表

| 层 | 选型 | 版本 | 选择理由 |
|---|---|---|---|
| 分布式存储 | HDFS | 3.3.6 | 承载 ODS/DWD/DWS 分层数据，练习真实大数据存储；本题数据量虽小，但链路要真 |
| 计算引擎 | Apache Spark (PySpark + Spark SQL) | 3.5.3 | 列式读写 + 分层建模 + 多维度聚合，是数仓作业的主力 |
| 业务数据库 | MySQL | 8.0 | 只存 ADS 服务层结果，供 Flask 快速查询 |
| 依赖管理 | Maven 坐标（`spark-submit --packages`） | — | 依赖可声明、可复现，替代早期手工 `--jars` 挂包 |
| 容器化 | Docker Compose | — | 5 容器一键拉起；**HDFS 镜像为自建 arm64**（官方仅 amd64，Mac M1 跑不了） |
| 服务端 | Flask + PyMySQL | — | 轻量 REST 服务，22 个接口，与前端完全解耦 |
| 前端（在线） | **Vue 3 + Vite + Pinia + Vue Router** | Vue 3.5 / Vite 6 | 前后端分离的标准工程形态，通过 `/api/*` 取数 |
| 前端（离线） | 原生 JS SPA + ECharts | 5.5.1 | 零依赖兜底产物，双击即开；见 3.5 |
| 数据抓取/治理 | Playwright + pandas | — | 百科词条需真实浏览器渲染（curl 直接 403） |

### 3.2 为什么「HDFS 存数仓 + MySQL 存服务层」而不是二选一

这是本项目架构上最关键的一个决定：

- **HDFS 存 ODS/DWD/DWS**：这三层是"数据加工过程"的产物，体量大、需要列式压缩与批量扫描，且要求可回溯（ODS 保留原始形态）。放 HDFS 是数仓的标准做法。
- **MySQL 只存 ADS**：服务层要支撑 Flask 的高频点查（按 id 取详情、按条件分页筛选），需要索引与低延迟。把 6MB 的明细表塞进 MySQL 反而拖慢一切。

一句话：**HDFS 负责"算得动"，MySQL 负责"查得快"**，两者职责不重叠。

### 3.3 为什么把作业按维度拆分

早期的实现是一个 407 行的单体 ETL 脚本 `spark/etl_hospital.py`，问题很明显：改一个维度的逻辑要通读全文，跑错一个环节要全量重来，代码里也看不出"分析了哪几个维度"。

现在拆成 `layer_ods / layer_dwd / dim_space / dim_category / dim_dept / dim_network / dim_time / layer_ads`，由 `run_all.py` 的 `STAGES` 表统一编排，可以 `--only deps` 只跑自检、`--from space` 从空间维度续跑。**维度即模块**，这与"按维度组织代码"的要求直接对应。

### 3.4 为什么用 Maven 坐标管理依赖

作业运行需要 MySQL JDBC 驱动。早期做法是手工下载 jar 再用 `--jars` 挂载 —— 换台机器就要重新找包，版本也说不清。

现在统一为在 `spark/pom.xml` 里声明坐标，运行时由 `spark-submit --packages com.mysql:mysql-connector-j:8.4.0` 交给 Ivy 解析，并缓存到挂载目录 `spark/.ivy2`，**答辩现场无网也能复用**。`pom.xml` 同时是依赖矩阵的权威声明，可直接用于论文附录。

### 3.5 前端为什么是「双形态」：Vue 工程 + 零依赖单文件

要求⑥是"Vue + Flask 前后端分离"。本项目**已按此落地**，同时刻意保留了原有的单文件形态，
两者并存、共用同一套后端接口：

| 形态 | 入口 | 取数方式 | 定位 |
|---|---|---|---|
| **Vue 在线形态** | `http://localhost:5001/spa/` | `GET /api/*` → Flask → MySQL | 标准前后端分离工程，源码在 `web/vue/` |
| **单文件离线形态** | `dashboard_offline.html` 或 `/` | 内联快照 `window.__SNAPSHOT__` | 零依赖兜底，双击即开 |

**Vue 工程的技术要点**（`web/vue/`，7 视图 / 6 组件 / 1 数据仓库）：

- `src/router.js` 用**哈希路由**：产物挂在 `/spa/` 子路径，哈希模式不需要服务端 history 回退，
  构建产物直接双击也能跑，兼顾了兜底能力。
- `src/store.js`（Pinia）是**唯一数据仓库**：查询条件、分页、距离基准点、对比选择、主题状态
  集中管理，页面组件不自行请求数据，保证"条件一变、全站同步"。
- `src/api.js` 是**唯一 HTTP 通道**，统一拦截响应与错误；前端不直连数据库、不读快照文件。
- `src/charts.js` 解决本项目最容易翻车的一点：**ECharts 画在 canvas 上，读不到 CSS 变量**。
  所有图表配色必须经 `tokens()` 把 `var(--x)` 解析成实色再喂给 `setOption`；主题切换后
  必须重新解析并重绘，否则暗色主题下仍会显示亮色配色。

**为什么不直接删掉单文件形态**：`dashboard_offline.html` 是一个 7.5 MB 的自包含文件，
双击即开、不依赖 Python/Docker/数据库/网络，在答辩现场与简历投递场景下极其可靠
（GitHub Pages 上可直接访问）。Vue 形态证明工程能力，单文件形态保证"一定能演示"，
两者不是替代关系，而是"在线标准形态 + 离线兜底"。

> 源码组织：`web/vue/src/views/` 七个视图一一对应侧栏导航项，
> `components/` 放 SideNav / TopBar / FilterBar / InstTable / DetailDrawer / CompareDialog。
> 构建：`bash web/vue/build.sh` → 产物 `web/vue/dist/`（已入库，Flask 挂在 `/spa/`）。

---

## 四、系统架构

### 4.1 七层架构

```
① 数据源层   data/ 70 个异构文件（医保局/区卫健委/专科名单）
                    │  清洗、合并、联网核实、地理编码
② 数据治理层 etl/*.py（56 个脚本）→ data/processed/*.csv
                    │  etl/upload_to_hdfs.py
③ 数据入湖层 HDFS /hospital/ods/raw（原始 CSV 落盘）
                    │  spark/jobs/layer_ods.py（列式化）
④ 计算层     HDFS /hospital/{ods/parquet, dwd, dws/{space,category,dept,network,time}}
                    │  spark/jobs/layer_ads.py
⑤ 存储层     MySQL hospital 库（19 张表，ADS 服务层）
                    │  web/app.py（22 个 REST 路由）
⑥ 服务层     JSON API over HTTP（Flask，默认 5001，22 个路由）
                    │  axios → /api/*   │   或 内联快照 __SNAPSHOT__
⑦ 展现层     双形态，共用同一套 API：
             · Vue 3 SPA（web/vue/，7 视图，挂载 /spa/）      ← 前后端分离形态
             · 单文件大屏（dashboard_offline.html，零依赖）    ← 离线兜底形态
```

### 4.2 部署拓扑（Docker Compose，5 容器）

| 容器 | 镜像 | 端口 | 作用 |
|---|---|---|---|
| `hdfs-namenode` | hospital-hdfs:3.3.6（**自建 arm64**） | 9870 / 8020 | HDFS 元数据与 RPC 入口 |
| `hdfs-datanode` | hospital-hdfs:3.3.6 | 9864 | HDFS 数据块存储 |
| `spark-master` | apache/spark:3.5.3 | 8080 / 7077 | Spark 提交入口；**默认单机模式下作业也在此容器内执行** |
| `spark-worker` | apache/spark:3.5.3 | 8081 | 可选：仅集群模式下参与计算 |
| `hospital-mysql` | mysql:8.0 | 3307→3306 | ADS 服务层业务库（宿主机 3306 被本地 MySQL 占用，故用 3307） |

> Spark 容器通过挂载 `docker/hadoop/conf/{core-site.xml,hdfs-site.xml}` 到 `/opt/spark/conf`（该目录在 Spark 默认 classpath 上）获得 HDFS 客户端能力；作业统一以 `hdfs://namenode:8020/hospital` 为根路径。

### 4.3 一次完整数据流转的耗时（2026-09-18 实测，单机模式 `local[*]`）

| 阶段 | 耗时 |
|---|---|
| ods 原始数据列式化 | 25.5 s |
| dwd 清洗与坐标关联 | 10.3 s |
| space 空间维度分析 | 24.3 s |
| category 类型与等级维度 | 16.0 s |
| dept 科室维度分析 | 10.7 s |
| network 协作网络维度 | 6.6 s |
| time 时间维度分析 | 16.5 s |
| ads 服务层落库 | 20.1 s |
| deps 环境自检 | 5.2 s |
| **合计（9 阶段）** | **135.2 s** |

---

## 五、数据资源与治理

### 5.1 数据来源

| 来源 | 说明 | 规模 |
|---|---|---|
| 北京市医保局定点医疗机构名录 | 含机构名称、地址、等级 | 4,876 条 |
| 社区卫生服务机构名录 | 社区卫生服务中心 + 服务站 | 1,972 条 |
| 各区卫健委公开名录及专题数据 | 区级机构明细 | 6,955 条 |
| 重点专科公示名单 | 国家级 / 市级临床重点专科 | 43 条 |
| 百度百科（在线核实） | 办别、等级、科室数、重点科室 | 2,000+ 词条访问 |

原始记录合计 13,803 条，按「机构名标准化 + 区县」去重后 9,791 条，最终主表 9,789 条。

### 5.2 治理流程

1. **盘点**：`inventory_sources.py` 以子进程隔离方式解析每个文件，防单文件崩溃拖垮全盘。
2. **读取**：`read_tables.py` 自动识别真 CSV（utf-8/gb18030，逗号/分号/制表符）、xlsx（含伪装成 .csv 的）、老 .xls。
3. **清洗合并**：`clean_merge.py` 字段映射 + 机构名标准化 + 去重。
4. **治理**：`govern_master.py` 补派生列（`grade_scope` 等）并清理脏行。
5. **在线核实**：见 5.3。
6. **地理编码**：`geocode_amap.py` 调高德 Geocoding 批量补经纬度，带缓存与限速。
7. **入湖**：`upload_to_hdfs.py` 把治理结果与行为日志灌入 HDFS，逐个比对字节数与行数。

### 5.3 在线核实（本项目数据可信度的主要来源）

| 核实项 | 方法 | 结果 |
|---|---|---|
| **办别** | 百度百科信息栏（医院性质 > 经济类型 > 经营性质）+ 名称规则引擎（军队系 25 / 国企职工 14 / 高校附属 31 / 政府办社区 20 / 区属直称 13） | 公立 **3,085** / 民营 **5,323** / 未标注 **1,381**（未标注从 1,514 降至 1,381） |
| **医院等级** | 同上网抓取 + 逐条比对 | 修正 2 家；三级 213 / 二级 212 / 一级 833 / 未定级 31 / 不适用医院分级 8,500 |
| **重点专科** | 权威名单（市卫健委 2018/2020 通知、2021/2022 疫情专项、2024/2025 单项 + 9 家头部医院官网）+ 212 家三级医院百科抓取 | 建立 `national_specialty` / `municipal_specialty` / `feature` 三级专科信号 |
| **科室数量** | 抓百科「主要科室/科室设置」章节，优先取"设置 N 个临床科室"这类权威声明 | 56 家在线核实（如 301 医院 165、中日友好 68、天坛 59），ADS 取值 `COALESCE(online, 源条目数)` |

**两个判定陷阱（已在代码中显式处理）**：
- 「主管部门 = 卫健委」**不等于**公立 —— 民营医院的监管单位同样登记卫健委，不能作为依据。
- 「医院性质 = 非公立」包含"公立"子串 —— 必须先判否定词再判关键词，否则会批量误判。

### 5.4 数据边界（如实披露，不粉饰）

| 现象 | 真实原因 | 处理方式 |
|---|---|---|
| 1,381 家办别未标注 | 多为村卫生室、个体诊所，公开渠道确无办别信息 | 保留"未标注"，标注 `ownership_src` 来源，不强行推测 |
| 8,500 家"不适用医院分级" | 诊所/村卫生室/门诊部等本身不参加医院等级评审 | 派生 `grade_scope` 字段区分，避免"未定级占比虚高"的误读 |
| 352 家民营医院等级缺失 | 百科词条中确无等级字段 | **如实保留为数据边界**，不补零、不猜测 |
| 248 家村卫生室未标注办别 | 村办/私人办混合，无法单一归类 | 有意保留 |

> 答辩口径：**缺失是数据的真实边界，而不是解析漏损**；系统通过"来源标注列"让每一条数据的可信度都可追溯。

---

## 六、数仓设计

### 6.1 分层规范

| 层 | 存储 | 职责 | 是否清洗 |
|---|---|---|---|
| ODS | HDFS `/hospital/ods/parquet` | 原始数据列式化，schema 与源文件一致 | **不清洗**，保证可回溯 |
| DWD | HDFS `/hospital/dwd` | 清洗 + 标准化 + 坐标关联 + 在线核实结果合并 | 清洗 |
| DWS | HDFS `/hospital/dws/<维度>` | 按 5 个分析维度轻度汇总 | 聚合 |
| ADS | MySQL `hospital` 库 | 面向业务系统的服务层结果 | 服务化 |
| META | HDFS `/hospital/meta/snapshots` | 每次跑批追加一条批次快照 | — |

### 6.2 DWD 层清洗规则

1. 机构 `id` 纯数字防御 + 按 id 去重
2. 区名去掉"北京"前缀（"北京市朝阳区"→"朝阳区"）
3. `level_norm` 等级归一化（含 `grade_scope = not_applicable` → "不适用医院分级"）
4. `category_norm` 类型归一化
5. 关联 geocode 坐标缓存（按 id 去重，防 join 膨胀）+ 坐标精度分桶（high / rough / missing）
6. **`COALESCE(online, 原值)`**：在线核实结果优先于源值 —— ⚠️ 该合并必须置于 `level_norm` 派生**之前**，否则归一化会基于旧值计算

### 6.3 五个分析维度（DWS）

| 维度 | 模块 | 产出表 | 行数 |
|---|---|---|---|
| 空间 | `dim_space.py` | `dws_inst_by_district` / `dws_district_center` / `dws_district_distance` | 16 / 16 / 256 |
| 类型与等级 | `dim_category.py` | `dws_inst_by_level` / `_by_category` / `_by_ownership` | 5 / 7 / 3 |
| 科室 | `dim_dept.py` | `dws_dept_coverage` / `_by_district` / `_density` | 29 / 377 / 16 |
| 协作网络 | `dim_network.py` | `dws_network_summary` / `_members` | 4 / 157 |
| 时间 | `dim_time.py` | `dws_time_daily` / `_event` / `_feature` | 6 / 14 / 6 |

其中 `dws_district_distance` 是 **16×16 的 Haversine 球面距离矩阵**，是"空间可达性"分析与距离筛选的数据基础。

### 6.4 ADS 服务层（MySQL 19 张表）

| 类别 | 表 |
|---|---|
| 服务副本（Flask 直接查） | `dwd_institution_clean`、`dwd_dept_relation_clean`、`dwd_specialty_clean`、`dws_inst_by_district`、`dws_inst_by_level`、`dws_inst_by_category`、`dws_dept_coverage` |
| 应用宽表 | `ads_inst_search`（9,789 × 42，检索主表）、`ads_district_overview`、`ads_level_overview`、`ads_specialty_hospital` |
| 分析结果 | `ads_network_summary`、`ads_time_trend`、`ads_time_feature`、`ads_etl_snapshot` |
| 维度表 | `dim_disease_dept`（导诊知识库 266 条）、`dim_poi_cache`（地理编码/周边缓存）、`dim_ai_cache`（大模型结果缓存） |
| 行为 | `fact_user_event`（埋点） |

**索引**：`ads_inst_search` 建 5 个索引（含协作网络四列联合索引），全库共 26 个索引，由 `etl/create_indexes.py` 统一维护。
> ⚠️ 落库使用 `mode="overwrite"`（DROP + CREATE），**每次重跑数仓后必须补跑 `create_indexes.py`**，否则筛选接口会退化为全表扫描。

---

## 七、功能模块

### 7.1 七个视图

| 视图 | 内容 |
|---|---|
| **数据总览** | 6 张 KPI 卡、北京 16 区地图撒点、机构类型构成、各区机构数 TOP、等级分布、办别构成、数据来源概览 |
| **医疗资源分析** | 多维度图表分析，随筛选条件联动重绘 |
| **机构查询** | 检索 + 分页列表（20/页）、行悬停 ↔ 地图互指、点行开详情抽屉、收藏 |
| **智能筛选** | 七维筛选控件 + 四种排序 + 常用预设 + 条件 chips + 结果预览 |
| **数据整合** | 多源整合流程、去重统计、字段补全说明 |
| **数据质量** | 字段完整率、数据边界披露 |
| **系统说明** | 数据来源、清洗流程、技术栈、口径说明 |

### 7.2 七维筛选与排序

**筛选维度**：① 关键词 ② 区域（16 区）③ 等级 ④ 类型 ⑤ 科室 ⑥ 协作网络 ⑦ 距离（Haversine，需设基准点）

**排序**：综合评分 / 等级 / 距离 / 机构名。其中综合评分权重为 **等级 0.5 + 距离 0.3 + 科室 0.2**。

> 注：智能导诊模块另有一套独立的推荐打分（未定位时 科室 0.45 / 专科 0.35 / 等级 0.20；已定位时 科室 0.30 / 专科 0.30 / 等级 0.20 / 距离 0.20），与主检索的排序权重**不是同一套**，材料撰写时勿混用。

### 7.3 关键算法

| 算法 | 位置 | 说明 |
|---|---|---|
| Haversine 球面距离 | `web/app.js` `haversine`、`web/app.py` `haversine` | 前端即时算距离保证手感，后端用于导诊与对比 |
| 地址解析五级降级 | `resolveBase` → `matchInstitutionBase` → `geocodeAddress` → `locateMe` → `autoLocate` | 地标 → 我的位置 → 区中心 → 高德 geocode → 机构名兜底；未设基准点时距离文案降级为「距市中心」 |
| 专科实力信号分级 | `ads_inst_search` 的 `national_specialty` / `municipal_specialty` / `feature` | 映射为 1.0 / 0.7 / 0.4 权重参与导诊打分 |
| 自然语言解析 | `parseNLQ` / `commitNLQ` | 本地规则解析区域、等级、类型、科室，含意图纠错（difflib 纠区名 + 27 条科室别名） |
| 生成式总结三级降级 | `genSummary` → 后端缓存 → `localSummary` | 大模型 → `dim_ai_cache` → 本地模板，**保证网络失败时绝不空白** |
| 地图 ↔ 列表悬停互指 | `SCATTER_IDX` + `ROW_EL_BY_ID` + `dispatchAction`（含去抖） | 9,789 点位下仍能无损联动 |

---

## 八、部署与运行

### 8.1 环境要求

- macOS（Apple Silicon）或 Linux，Docker 24+、Docker Compose v2
- Python 3.13（治理与构建脚本）、Node 22（仅前端构建辅助）
- 内存建议 ≥ 8 GB（Spark 容器 4 GB + HDFS + MySQL）

### 8.2 从零到跑起来（7 步）

```bash
# 1. 启动 5 个容器
docker compose up -d

# 2. 治理数据入湖到 HDFS（ODS 原始层，含行为日志导出）
python3 etl/upload_to_hdfs.py

# 3. 数仓全链路：ODS → DWD → 五维度分析 → ADS 落 MySQL（默认单机模式）
bash spark/run_all.sh

# 4. 重建查询索引（落库是 overwrite，索引必丢，此步必做）
python3 etl/create_indexes.py

# 5. 重建单文件前端快照与三份产物
bash web/build_spa.sh

# 6. 构建 Vue 前端（前后端分离形态 → web/vue/dist，Flask 挂载在 /spa/）
bash web/vue/build.sh

# 7. 启动 Web 服务
bash web/start.sh          # Vue: http://localhost:5001/spa/   离线单文件: http://localhost:5001/
```

> 第 6 步只有在改动 `web/vue/src/` 后才必须重跑；`web/vue/dist/` 已入库，
> 只 clone 不改前端的话直接进第 7 步即可访问 `/spa/`。

### 8.3 两种运行模式

```bash
# 默认：单机模式（Driver 在本容器内多线程并行）
bash spark/run_all.sh

# 可选：Standalone 集群模式（同一份代码、同一份 Maven 坐标，仅换 --master）
SPARK_MASTER=spark://spark-master:7077 bash spark/run_all.sh
```

单机模式下作业在 `spark-master` 容器内执行，依赖仍由 Maven 坐标解析（`--packages` + Ivy 缓存），因此**"Maven 单机模式"的两个要素同时成立**：依赖以 Maven 声明、运行以单机方式。

### 8.4 只跑一部分

```bash
bash spark/run_all.sh --only deps      # 只跑环境自检
bash spark/run_all.sh --only ads       # 只重落服务层
bash spark/run_all.sh --from space     # 从空间维度续跑
```

---

## 九、测试与验证

本章的三条命令是**答辩现场证明"链路是真的"的核心手段**——不需要相信文档，跑一遍即可。

### 9.1 链路自检：`spark/jobs/check_deps.py`

```bash
bash spark/run_all.sh --only deps
```

实测输出（2026-09-18）：

```
1. Spark 运行模式
  master = local[*]
  ✓ 单机模式 local[*]（复盘标准④：Maven 单机模式，依赖同样由 Maven 坐标解析）
2. HDFS 数仓分层可读性与行数
  ✓ /hospital/ods/parquet/ods_institution      9789 行
  ✓ /hospital/dwd/dwd_institution_clean        9789 行
  ✓ /hospital/dws/space/dws_district_distance   256 行
  ... 共 23 张分层表全部可读
3. Maven 依赖 + MySQL 连通性
  ✓ jdbc 驱动可用，ads_inst_search = 9789 行
✅ 自检全部通过
```

### 9.2 产物一致性：`etl/verify_artifacts.py`

三份前端产物是"字符串注入"式拼装的，锚点错位会**静默产出坏页面**（能打开、不报错，只是没数据）。本脚本校验体积区间、快照赋值次数、内联 JSON 可解析性、机构条数、离线版是否真的内联了 ECharts、有无 IDE 预览脏标记残留。

```bash
python3 etl/verify_artifacts.py
# ✅ 三份产物 + Vue 产物校验通过（9,789 家 / 快照哈希一致）
```

同一脚本还顺带校验 **Vue 构建产物**：`dist/index.html` 引用的资源是否都在、
七个视图 chunk 是否各自成块（防止"少打包了一个视图"这种静默失败）、
产物是否旧于源码（源码比 `dist` 新会直接告警提示重跑构建）。

### 9.2.1 Vue 端到端冒烟：`etl/smoke_vue.py`

```bash
# 先确保 Flask 在跑，然后：
python3 etl/smoke_vue.py
# ✅ 全部通过（7/7 路由、9 个图表 canvas、筛选 9,789→1,315、0 运行时错误）
```

用无头 Chrome 真实驱动 Vue 应用：逐条访问 7 个路由并检查渲染内容、
统计 ECharts canvas 数量、在机构查询页真实触发下拉筛选与翻页、
点开详情抽屉、切换主题后确认图表重绘，并断言全程无未捕获异常。

### 9.3 无头冒烟：`etl/smoke_dashboard.py`

用无头 Chrome 真跑一遍页面（打桩 ECharts 以避免 headless 虚拟时间不收敛）：

```bash
python3 etl/smoke_dashboard.py
```

实测输出（2026-09-18）：

```
snap=9789 ;; views=7 ;; nav=7 ;; kpi_total=9,789
switch=overview+,analytics+,institutions+,filter+,integration+,quality+,about+
rows_inst=20 ;; filtered=9789_to_1315 ;; ERR=none
✅ 冒烟通过：数据加载 + 七视图渲染 + 筛选联动 全部正常
```

其中 `filtered=9789_to_1315` 是**按昌平区筛选后命中 1,315 家**，与该区实际机构数完全吻合 —— 说明筛选确实是数据驱动的，不是写死的。

### 9.4 数据层一致性抽查

```bash
# MySQL 侧（端口 3307）
SELECT ownership, COUNT(*) FROM ads_inst_search GROUP BY ownership;
# 民营 5323 / 公立 3085 / 未标注 1381
SELECT level_norm, COUNT(*) FROM ads_inst_search GROUP BY level_norm;
# 不适用医院分级 8500 / 一级 833 / 三级 213 / 二级 212 / 未定级 31
```

---

## 十、指导老师验收要求对照表

| # | 要求 | 状态 | 落地位置 / 说明 |
|---|---|---|---|
| ① | 数据分析读取的数据来自 **HDFS** | ✅ 达成 | `etl/upload_to_hdfs.py` → `/hospital/ods/raw`；`layer_ods.py` 从 HDFS 读；`check_deps.py` 打印 23 张分层表行数 |
| ② | 数据预处理的中间结果**回写 HDFS** | ✅ 达成 | `layer_dwd.py` 写 `/hospital/dwd`；`dim_*.py` 写 `/hospital/dws/<维度>`；`common.py` `write_parquet()` 统一落地 |
| ③ | 数据预处理与分析使用**大数据技术**（Spark / Hive） | ✅ 达成（Spark） | Spark 3.5.3 承担全部清洗、关联、聚合；**未引入 Hive**，理由见下方说明 |
| ④ | 依赖以 **Maven** 方式管理，作业以**单机模式**运行 | ✅ 达成 | `spark/pom.xml` 声明坐标；`run_all.sh` 默认 `--master local[*]`，`--packages` 由 Ivy 解析并缓存 |
| ⑤ | 分析结果保存到**业务数据库**（MySQL） | ✅ 达成 | `layer_ads.py` 落 19 张表；`ads_inst_search` 9,789 × 42 |
| ⑥ | 前后端分离（**Vue + Flask**） | ✅ 达成 | 前端 `web/vue/`（Vue 3 + Vite + Pinia + Router，7 视图 / 6 组件），经 `/api/*` 调 Flask，产品挂载在 `/spa/`；后端 22 路由返回纯 JSON，不含任何模板拼装。冒烟见 `etl/smoke_vue.py` |
| ⑦ | 代码**按维度组织** | ✅ 达成 | `spark/jobs/` 按分层 + 5 个分析维度拆为 10 个模块；`run_all.py` 的 `STAGES` 表可一眼看全 |
| ⑧ | 生成**项目说明书**与论文材料 | ✅ 达成 | 本说明书 + 《功能点-代码对应表》+《系统运行流程》+《毕业论文》+《答辩与简历讲解手册》 |
| ⑨ | 整理**功能点 → 代码位置 / 流程 / 作用** | ✅ 达成 | `docs/功能点-代码对应表.md` |

### 关于 ③ 中 Hive 的取舍说明（需在答辩时主动说明）

本项目**未使用 Hive Metastore**，数仓建模完全由 **Spark SQL + HDFS Parquet** 承担。理由：

1. **职责重叠**：Hive 的核心价值是"用 SQL 管理 HDFS 上的表"，而 Spark SQL 已经具备等价的 Catalog 能力（`spark.sql` 直接 `CREATE TABLE ... USING parquet LOCATION 'hdfs://...'`），再叠一层 Hive Metastore 对本项目的数据规模（万级）没有实际收益，只会增加一个故障点。
2. **一致性**：全部数据处理集中在 Spark 一处，口径不会在"Hive 侧 SQL"与"Spark 侧 DataFrame"之间漂移。
3. **可扩展性**：如果后续需要 Hive，只需在 `spark-submit` 加 `--conf spark.sql.catalogImplementation=hive` 并把 DWS 层的建表语句从 DataFrame 写法换成 `CREATE TABLE` 即可，现有分层结构（ODS/DWD/DWS/ADS）无需改动。

**建议答辩表述**："数据预处理与分析使用 Spark 完成；数仓分层由 Spark SQL 建模在 HDFS 上，未额外引入 Hive Metastore，原因是职责重叠且本项目规模下无收益。如需 Hive，可通过开启 `spark.sql.catalogImplementation=hive` 平滑接入，分层结构不变。"

---

## 十一、后续改进

### 11.1 前端 Vue 化（已完成，对应要求⑥）

原计划见本节旧稿：新建 `web/vue/`（Vite + Vue 3），复用现有 `/api/*`，构建产物由 Flask 挂载到
`/spa`，现有 `templates/index.html` 与 `dashboard_offline.html` 原样保留。**该方案已完整实施**，
映射关系与预期一致：

| 原有（原生 JS） | 实施结果（Vue 3） |
|---|---|
| 7 个 `render*` 函数 | 7 个视图组件 `src/views/*.vue` |
| `DATA` / `FILTERED` 全局状态 | Pinia store `useDataStore`（`src/store.js`） |
| `applyFilter()` 全量重绘 | `computed` 派生 + 细粒度响应式更新 |
| `bindEvents()` 集中绑定 | 组件内 `@click` / `v-model` |
| `dashboard.html` 的静态 section | `App.vue` + `vue-router`（哈希模式） |
| `/api/*` fetch | axios 封装的 `src/api.js`（**接口未做任何改动**） |

**验证结果**：`etl/smoke_vue.py`（无头 Chrome 真实驱动）7/7 条路由渲染正常、
4+4+1 个 ECharts 实例成功绘制、机构列表分页可翻、详情抽屉可开、
区域筛选 `9,789 → 1,315 家` 与数据层一致、主题切换后图表重绘正常、**0 条运行时错误**。

**遗留可选项**（不影响验收）：把 `web/vue/dist/` 从仓库移除、改为构建期生成，
或引入 `manualChunks` 把 ECharts 单独拆包（当前 ECharts chunk 约 1.0 MB）。

### 11.2 其他

- **Hive 可选接入**（同 10.3 的说明）
- **时间维度数据积累**：`ads_time_trend` 目前数据量不足（行为日志需长期累积），趋势分析页暂未接入前端
- **协作网络/导诊/运营三页**：v5.0 信息架构调整后从导航下线，**后端接口与前端函数均保留**（守卫式降级），如需要可随时恢复入口

---

## 附录 A：目录结构

```
毕设/
├── data/                    数据层（70 个源文件 + processed/ 治理产物）
├── etl/                     数据治理脚本（56 个 Python）
│   ├── clean_merge.py       多源清洗整合
│   ├── govern_master.py     主表治理
│   ├── build_*_online.py    在线核实合并（办别/等级/专科/科室数）
│   ├── scrape_baike_*.py    百科抓取（Playwright）
│   ├── geocode_amap.py      高德地理编码
│   ├── upload_to_hdfs.py    数据入湖 HDFS
│   ├── create_indexes.py    MySQL 索引维护
│   ├── verify_artifacts.py  三份产物 + Vue 产物一致性校验
│   ├── smoke_dashboard.py   单文件大屏无头冒烟
│   └── smoke_vue.py         Vue 前端端到端冒烟（7 路由）
├── spark/                   数仓核心
│   ├── pom.xml              Maven 依赖声明（权威）
│   ├── run_all.sh           一键跑批（默认单机模式）
│   └── jobs/                10 个作业模块
│       ├── common.py        公共模块（路径/会话/读写）
│       ├── layer_ods.py     ODS 层
│       ├── layer_dwd.py     DWD 层
│       ├── dim_space.py     空间维度
│       ├── dim_category.py  类型等级维度
│       ├── dim_dept.py      科室维度
│       ├── dim_network.py   协作网络维度
│       ├── dim_time.py      时间维度
│       ├── layer_ads.py     ADS 服务层
│       ├── check_deps.py    环境自检
│       └── run_all.py       编排入口
├── web/                     应用层
│   ├── app.py               Flask 服务端（22 路由）
│   ├── dashboard.html       前端骨架（源）
│   ├── app.js               前端逻辑（源，3,314 行）
│   ├── build_spa.sh         快照与三产物构建
│   ├── start.sh             Flask 启动
│   ├── dashboard_offline.html   交付物①：完全离线（零依赖）
│   ├── dashboard_standalone.html 交付物②：在线静态
│   ├── templates/index.html     交付物③：Flask 渲染
│   └── vue/                 Vue 3 前端工程（前后端分离，挂载于 /spa/）
│       ├── src/             源码：7 视图 + 6 组件 + api/store/charts
│       ├── build.sh         一键构建 / dev server
│       ├── README.md        工程说明与排错
│       └── dist/            构建产物（已入库，Flask 挂载点）
├── docker/hadoop/           自建 arm64 HDFS 镜像
├── docker-compose.yml       5 容器编排
└── docs/                    项目文档
    ├── 项目说明书.md            （本文）
    ├── 功能点-代码对应表.md      要求⑨
    ├── 系统运行流程.md
    ├── 毕业论文.md
    ├── 答辩与简历讲解手册.md
    └── 开发日志.md
```

## 附录 B：数据表规模一览

| 数据 | 规模 |
|---|---|
| `master_institutions.csv`（主表） | 9,789 行 × 42 列 |
| `hospital_depts.csv`（机构-科室关系） | 16,251 行 × 6 列 |
| `dept_dict.csv`（标准科室字典） | 32 个科室 |
| `specialty_departments.csv`（重点专科名单） | 43 条 |
| `geocode_cache.csv`（坐标缓存） | 9,791 行 × 7 列 |
| `disease_dept_map.csv`（导诊知识库） | 266 条 |
| MySQL `hospital` 表数 | 19 张 |
| MySQL 索引数 | 26 个 |

## 附录 C：术语表

| 术语 | 含义 |
|---|---|
| ODS / DWD / DWS / ADS | 数仓四层：原始层 / 明细层 / 汇总层 / 服务层 |
| DWS | Data Warehouse Summary，按分析维度轻度汇总 |
| 办别 | 医疗机构的主办性质（公立 / 民营 / 未标注） |
| grade_scope | 机构是否属于"参加医院等级评审"的范围（决定"未定级"是否可比） |
| 核心 / 成员 | 协作网络中医联体的牵头医院与成员医院 |
| 七维筛选 | 关键词 / 区域 / 等级 / 类型 / 科室 / 协作网络 / 距离 |
| 基准点 | 距离计算的原点（用户设定地址或自动定位结果） |
| 空桩 | 测试时替换掉 ECharts 的空实现，避免 headless 浏览器动画不收敛 |

---

*本文与 `README.md`、`docs/功能点-代码对应表.md`、`docs/毕业论文.md` 口径一致，所有指标均取自真实数据与实测输出。*
